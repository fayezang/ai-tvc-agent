# AI TVC Agent — 环境变量示例
#
# 复制为 .env 后按需填写。.env 已被 .gitignore 忽略，不会进入版本控制。
#
# ⚠️ 重要：正常使用本应用【不需要】这个文件。
#
# ORZ API Key 由用户在应用内「模型与 API」界面录入，经 Electron 的
# safeStorage 加密后存入操作系统安全存储（macOS 钥匙串 / Windows DPAPI），
# 全程不写入任何文件，Renderer 进程也无法读取。
#
# 本文件仅用于两种场景：
#   1. 开发调试时跳过 UI 录入
#   2. 记录可配置项的完整清单，便于迁移到新机器时核对
#
# 迁移到新电脑时不要迁移密钥：旧机钥匙串中的内容无法也不应导出，
# 在新机器的应用界面重新录入一次即可。

# ORZ 网关 API Key。获取地址：https://orz.sh
# 留空即可 —— 请在应用界面录入。
ORZ_API_KEY=

# ORZ 网关地址。除非官方变更，否则保持默认值。
ORZ_BASE_URL=https://orz.sh/api/proxy/v1
